# RegistrationApp project by Anurag Satapathy

#  Implementation of the CRUD operations
#   Backend code changes
#    Implemented the CRUD operations for the registration app and it includes the java changes and mysql code changes.
  #    Files are CreateOperation, which inserts the registrations details into the Registration table.
   #    ReadOpeartion, which selects the registration details from the Registration table.
   #    UpdateOpearation, which updates the existing records in the Registration table.
   #    DeleteOperation, which deletes the existing record in the Registration table.
   #    JDBCUtil class, which handles connections,extablishment with the database driver and closing of the resources.The 


#   MySql changes
   #    The create table script has been placed in the file for the RegistrationApp.
